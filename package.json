{
  "name": "db-compat",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@10.5.2",
  "scripts": {
    "build": "pnpm -r run build && ([ -d website/.vercel/output ] && cp -r website/.vercel/output .vercel/output || true)",
    "test": "vitest",
    "test:target": "vitest run --project",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "generate": "bash scripts/generate.sh",
    "generate:node": "tsx scripts/generate.ts",
    "generate:d1": "tsx scripts/generate-d1.ts",
    "generate:hyperdrive": "tsx scripts/generate-hyperdrive.ts",
    "generate:types": "tsx scripts/generate-types.ts"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:lint",
    "@electric-sql/pglite": "catalog:db",
    "@libsql/client": "catalog:db",
    "@planetscale/database": "catalog:db",
    "@types/better-sqlite3": "catalog:",
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "better-sqlite3": "catalog:db",
    "db0": "catalog:db",
    "eslint": "catalog:lint",
    "miniflare": "catalog:",
    "mysql2": "catalog:db",
    "pg": "catalog:db",
    "sqlite3": "catalog:db",
    "tsdown": "catalog:build",
    "tsx": "catalog:",
    "typescript": "catalog:types",
    "vitest": "catalog:test"
  }
}
